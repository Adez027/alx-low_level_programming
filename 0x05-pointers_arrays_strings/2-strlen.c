#include "main.h"

/**
 * _puts - prints a string to stdout
 * @str: string to print
 * Return: void
 */
void _puts(char *str)
{
	int e = 0;

	while(str[e]!='\0')
	{
		_putchar(str[e]);
		e++;
	}
	_putchar('\n');
}

	while(str[l] != '\0')
	{
		_putchar(str[l]);
		l++;
	}
	_putchar('\n');
}
